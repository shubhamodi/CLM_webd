<section>
  <div
    class="mx-auto w-full lg:px-24 max-w-7xl md:px-12 items-center px-8 py-24 scroll-mt-12"
  >
    <div class="grid grid-cols-1 lg:grid-cols-3 gap-6 lg:gap-12">
      <div class="text-center lg:text-left">
        <div>
          <p
            class="text-3xl font-normal tracking-tight text-[#000000] font-display"
          >
            Frequently asked questions
          </p>
          <p class="text-base mt-4 text-zinc-300">
            Answers to commonly asked questions about our platform
          </p>
        </div>
      </div>
      <div
        x-data="{
  activeAccordion: '',
  setActiveAccordion(id) {
  this.activeAccordion = (this.activeAccordion == id) ? '' : id
  }
  }"
        class="relative w-full mx-auto font-normal lg:col-span-2"
      >
        <!-- FAQ Item 1 -->
        <div
          x-data="{ id: $id('accordion') }"
          :class="{ 'text-[#000000]': activeAccordion==id, 'text-zinc-400 hover:text-accent-500': activeAccordion!=id }"
          class="cursor-pointer group text-[#000000]"
        >
          <button
            @click="setActiveAccordion(id)"
            class="flex items-center justify-between w-full p-4 pb-1 text-left font-medium select-none"
          >
            <span>What is the Technology Behind ContraVault?</span>
            <svg
              class="w-5 h-5 duration-300 ease-out text-accent-500"
              :class="{ '-rotate-[45deg]': activeAccordion==id }"
              xmlns="http://www.w3.org/2000/svg"
              fill="none"
              viewBox="0 0 24 24"
              stroke-width="1.5"
              stroke="currentColor"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                d="M12 6v12m6-6H6"></path>
            </svg>
          </button>
          <div x-show="activeAccordion==id" x-collapse="">
            <div class="p-4 pt-2 text-zinc-300">
              ContraVault leverages advanced AI technology, specifically refined
              Large Language Models (LLMs) designed for complex legal analysis
              and contract management. These LLMs analyze your contracts,
              identify relevant clauses, flag potential compliance issues, and
              make context-driven recommendations to ensure your documents align
              with your specific legal guidelines.
            </div>
          </div>
        </div>
        <!-- FAQ Item 2 -->
        <div
          x-data="{ id: $id('accordion') }"
          :class="{ 'text-[#000000]': activeAccordion==id, 'text-zinc-400 hover:text-accent-500': activeAccordion!=id }"
          class="cursor-pointer group text-zinc-400 hover:text-accent-500"
        >
          <button
            @click="setActiveAccordion(id)"
            class="flex items-center justify-between w-full p-4 pb-1 text-left font-medium select-none"
          >
            <span>How Does ContraVault Protect My Data?</span>
            <svg
              class="w-5 h-5 duration-300 ease-out text-accent-500"
              :class="{ '-rotate-[45deg]': activeAccordion==id }"
              xmlns="http://www.w3.org/2000/svg"
              fill="none"
              viewBox="0 0 24 24"
              stroke-width="1.5"
              stroke="currentColor"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                d="M12 6v12m6-6H6"></path>
            </svg>
          </button>
          <div
            x-show="activeAccordion==id"
            x-collapse=""
            style="display: none;"
          >
            <div class="p-4 pt-2 text-zinc-300">
              Your data security is our top priority. ContraVault ensures data
              privacy and protection in the following ways:
              <ul class="list-disc list-inside mt-2">
                <li>
                  Data Handling: ContraVault only accesses the document you’re
                  actively working on.
                </li>
                <li>
                  Data Storage: Information sent to the AI model is processed
                  securely, with compliance to GDPR and other leading privacy
                  regulations.
                </li>
                <li>
                  Microsoft Privacy: Any document data processed using our
                  models is not used by Microsoft for model training purposes.
                </li>
                <li>
                  User Logs: We maintain logs for troubleshooting and support,
                  with an option to delete user data upon request.
                </li>
                <li>
                  Secure Access: Your login credentials are safely stored and
                  managed via enterprise-grade solutions.
                </li>
              </ul>
            </div>
          </div>
        </div>
        <!-- FAQ Item 3 -->
        <div
          x-data="{ id: $id('accordion') }"
          :class="{ 'text-[#000000]': activeAccordion==id, 'text-zinc-400 hover:text-accent-500': activeAccordion!=id }"
          class="cursor-pointer group text-zinc-400 hover:text-accent-500"
        >
          <button
            @click="setActiveAccordion(id)"
            class="flex items-center justify-between w-full p-4 pb-1 text-left font-medium select-none"
          >
            <span>What File Formats Does ContraVault Support?</span>
            <svg
              class="w-5 h-5 duration-300 ease-out text-accent-500"
              :class="{ '-rotate-[45deg]': activeAccordion==id }"
              xmlns="http://www.w3.org/2000/svg"
              fill="none"
              viewBox="0 0 24 24"
              stroke-width="1.5"
              stroke="currentColor"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                d="M12 6v12m6-6H6"></path>
            </svg>
          </button>
          <div
            x-show="activeAccordion==id"
            x-collapse=""
            style="display: none;"
          >
            <div class="p-4 pt-2 text-zinc-300">
              ContraVault supports .pdf, .docx, .txt, and .xlsx file formats to
              ensure seamless integration with modern document standards. Note
              that .doc files and documents in compatibility mode are not
              supported.
            </div>
          </div>
        </div>
        <!-- Add other FAQ items here in the same format -->
      </div>
    </div>
  </div>
</section>
